{
    "name": "android-server",
    "version": "1.0.0",
    "description": "Android Device Management Server",
    "main": "main.js",
    "scripts": {
        "start": "node main.js",
        "dev": "nodemon main.js --watch server/** --watch config/** --watch main.js --watch settings.cfg --ext js,cfg --ignore 'public/**' --ignore '*.test.js' --ignore 'node_modules/*' --delay 500"
    },
    "dependencies": {
        "express": "^4.18.2",
        "multer": "^1.4.5-lts.1"
    },
    "devDependencies": {
        "nodemon": "^3.0.2"
    }
}
